#spring:
#  cloud:
#    config:
##   按顺序覆盖，多个一样的配置，后者覆盖前者后面优先级最高
#      name: common-datasource,common-eureka,auth
#      label: master
#      profile: dev
#      discovery:
#        enabled: true
#        service-id: config-server